plugins {
    id("email.haemmerle.baseplugin").version("1.0.2-5-g089b77c").apply(false)
}

group = "email.haemmerle.baseplugin"
description = "Gradle base plugin for kotlin projects and the CI chain github -> travis -> bintray"